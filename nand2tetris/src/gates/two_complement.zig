const std = @import("std");
const t = std.testing;

const u = @import("utils.zig");
const b4 = u.b4;
const b16 = u.b16;

// pub inline fn TWO_COMPLEMENT(in: [4]u1) [4]u1 {
//     return [4]u1{ 1, 0, 0, 0 };
// }
